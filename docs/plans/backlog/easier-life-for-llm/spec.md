When the heartbeat session is invoked, I want to implement 
programmatically the git pull (with possible stash of local changes), 
so the agent does not need to bother with it manually.
Also, after the heartbeat session, automatic push should be performed, 
but the AI assistant should be left to make the commits if needed.

This way, the agent does not need to bother with pulling and pushing operations.
